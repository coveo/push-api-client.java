name: 'CodeQL'

on:
  push:
    branches: ['main']

  pull_request:
    branches: ['main']

  schedule:
    - cron: '29 3 * * 6' # Runs at 03:29, only on Saturday.

jobs:
  analyze-java:
    uses: coveo/public-actions/.github/workflows/java-maven-openjdk11-codeql.yml@8ce6eca365702b0ad9f463a01135ba88ae6c43a8 # main
    with:
      runs-on: '"ubuntu-latest"'
